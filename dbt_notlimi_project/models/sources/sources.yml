# pour générer la doc
#dbt run-operation generate_source --args '{"schema_name": "notlimitail_datadubrazil", "table_names": ["customers", "orders", "order_item", "orders_payment", "products", "sellers"]}'

version: 2

sources:
# C'est le nom de notre source, plus clair.
  - name: olist_ecommerce  
    description: "Données brutes Olist E-commerce chargées depuis GCS."
    
    # On met le nom du dataset qui existe VRAIMENT dans BigQuery
    schema: notlimitail_datadubrazil
    
    # On liste les tables avec leurs noms exacts
    tables:
      - name: customers
      - name: orders
      - name: order_items     
      - name: order_payments
      - name: products
      - name: sellers